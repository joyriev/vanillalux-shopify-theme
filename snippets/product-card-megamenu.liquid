<a class="no-underline block" href="{{ product.url }}">
  <img
    class="w-full h-auto"
    src="{{ product.featured_image.src |  image_url: width: 300 }}"
    alt="{{ product.featured_image.alt }}"
    height="{{ product.featured_image.height }}"
    width="{{ product.featured_image.width }}">
  <div class="text-gray-500 text-sm leading-none tracking-normal">
    Smells like
  </div>
  <div class="line-clamp-1 tracking-normal font-semibold text-black text-base">
    {{ product.title | remove: 'Smells like...' | remove: 'Smells like ...' }}
  </div>
  {%- if product.metafields.reviews.rating.value != blank -%}
    <div
      class="rating"
      role="img"
      aria-label="{{ 'accessibility.star_reviews_info' | t: rating_value: product.metafields.reviews.rating.value, rating_max: product.metafields.reviews.rating.value.scale_max }}">
      <img
        src="{{ 'rating-stars.svg' | asset_url }}"
        alt="*****"
        height="13"
        width="66">
      <span class="text-sm tracking-tight text-gray-950">({{ product.metafields.reviews.rating_count }})</span>
    </div>
  {%- endif -%}
  <div class="font-bold tracking-normal text-black">
    {% unless product.tags contains 'onsale' %}
      <span class="starts_at_text">Starts at</span>
    {% endunless %}
    {{ product.price | money }}
    {% if product.tags contains 'onsale' %}
      <span class="line-through text-[#c8202f] font-normal">
        {{ cart.currency.symbol }}{{ product.compare_at_price | money_without_currency }}</span>
    {% endif %}
  </div>
</a>